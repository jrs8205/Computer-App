<Window x:Class="HardwareMonitor.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:app="clr-namespace:HardwareMonitor.App"
        xmlns:vm="clr-namespace:HardwareMonitor.App.ViewModels"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
        Title="Windows 11 Hardware Monitor — sensoritestaus (PoC)"
        Height="760" Width="960"
        Background="#1E1E1E"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Laite (ja alalaite) puun solmuna. Lapset tulevat Items-listasta. -->
        <HierarchicalDataTemplate DataType="{x:Type vm:HardwareViewModel}"
                                  ItemsSource="{Binding Items}">
            <TextBlock Text="{Binding Header}"
                       FontWeight="Bold"
                       Foreground="#4FC3F7"
                       Margin="0,3" />
        </HierarchicalDataTemplate>

        <!-- Raja-arvotila -> väri (vihreä/oranssi/punainen) -->
        <app:ThresholdStateToBrushConverter x:Key="StateBrush" />

        <!-- Dashboard-korttien tyylit -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="#252526" />
            <Setter Property="CornerRadius" Value="8" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="8" />
            <Setter Property="MinWidth" Value="220" />
        </Style>
        <Style x:Key="CardTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="#4FC3F7" />
            <Setter Property="Margin" Value="0,0,0,8" />
        </Style>
        <Style x:Key="CardLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#BDBDBD" />
            <Setter Property="Margin" Value="0,2" />
        </Style>
        <Style x:Key="CardValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#A5D6A7" />
            <Setter Property="FontFamily" Value="Consolas" />
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="Margin" Value="24,2,0,2" />
        </Style>

        <!-- Yksittäinen sensori: nimi | arvo | tyyppi -->
        <DataTemplate DataType="{x:Type vm:SensorViewModel}">
            <Grid Margin="0,1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding Name}" Foreground="#E0E0E0" />
                <TextBlock Grid.Column="1" Text="{Binding DisplayValue}"
                           Foreground="#A5D6A7" HorizontalAlignment="Right" Margin="0,0,20,0" />
                <TextBlock Grid.Column="2" Text="{Binding Type}" Foreground="#BDBDBD" />
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <DockPanel>
        <!-- Yläpalkki -->
        <Border DockPanel.Dock="Top" Background="#252526" Padding="16,12">
            <StackPanel>
                <TextBlock Text="Windows 11 Hardware Monitor"
                           FontSize="20" FontWeight="Bold" Foreground="White" />
                <TextBlock Text="Proof of concept — kaikki löydetyt sensorit, arvot päivittyvät 1 sekunnin välein"
                           Foreground="#BDBDBD" Margin="0,2,0,0" />
                <WrapPanel Margin="0,10,0,0">
                    <CheckBox Content="Overlay työpöydälle" IsChecked="{Binding OverlayEnabled}"
                              Foreground="White" VerticalAlignment="Center" />
                    <CheckBox x:Name="MoveOverlayCheck" Content="Siirrä overlayta"
                              IsEnabled="{Binding OverlayEnabled}"
                              Checked="MoveOverlay_Checked" Unchecked="MoveOverlay_Unchecked"
                              Foreground="White" Margin="16,0,0,0" VerticalAlignment="Center"
                              ToolTip="Kun päällä, overlayn voi raahata hiirellä haluamaansa paikkaan" />
                    <Button Content="Luo raportti…" Click="CreateReport_Click"
                            Margin="24,0,0,0" Padding="10,3" VerticalAlignment="Center"
                            ToolTip="Selkokielinen yhteenveto koneen kunnosta tekstitiedostona" />
                    <Button Content="Vie CSV…" Click="ExportCsv_Click"
                            Margin="8,0,0,0" Padding="10,3" VerticalAlignment="Center"
                            ToolTip="Viimeisen 24 tunnin sensorihistoria Exceliin sopivana CSV:nä" />
                </WrapPanel>
            </StackPanel>
        </Border>

        <!-- Alapalkki: tila + lokipolku -->
        <Border DockPanel.Dock="Bottom" Background="#004C87" Padding="16,8">
            <StackPanel>
                <TextBlock Text="{Binding Status}" Foreground="White" FontWeight="SemiBold" />
                <TextBlock Text="{Binding LogPath, StringFormat=Debug-loki: {0}}"
                           Foreground="#DCF3FF" FontSize="11" Margin="0,2,0,0" />
            </StackPanel>
        </Border>

        <!-- Sisältö: Dashboard-kortit + raakasensoripuu välilehtinä -->
        <TabControl Background="#1E1E1E" BorderThickness="0">
            <TabItem Header="Dashboard">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="8" DataContext="{Binding Dashboard}">

                        <!-- Tilapaneeli: riskianalyysin yhteenveto + väriselite (luku 19) -->
                        <Border Background="#252526" CornerRadius="8" Padding="14,12" Margin="8,8,8,4">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal">
                                    <Ellipse Width="14" Height="14" VerticalAlignment="Center"
                                             Fill="{Binding SummaryState, Converter={StaticResource StateBrush}}" />
                                    <TextBlock Text="{Binding SummaryTitle}" FontSize="16" FontWeight="Bold"
                                               Foreground="White" Margin="10,0,0,0" VerticalAlignment="Center" />
                                </StackPanel>
                                <TextBlock Text="{Binding SummaryObservations}" Foreground="#CCCCCC"
                                           Margin="24,8,0,0" TextWrapping="Wrap" LineHeight="20" />
                                <TextBlock Foreground="#FFB74D" FontWeight="SemiBold"
                                           Margin="24,8,0,0" TextWrapping="Wrap">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="{Binding SummaryRecommendation}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding SummaryRecommendation}" Value="">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <WrapPanel Margin="24,10,0,0">
                                    <TextBlock Text="Värien selitys:" Foreground="#BDBDBD" FontSize="11"
                                               Margin="0,0,10,0" VerticalAlignment="Center" />
                                    <Ellipse Width="10" Height="10" Fill="#A5D6A7" VerticalAlignment="Center" />
                                    <TextBlock Text="kunnossa" Foreground="#BDBDBD" FontSize="11"
                                               Margin="4,0,14,0" VerticalAlignment="Center" />
                                    <Ellipse Width="10" Height="10" Fill="#FFB74D" VerticalAlignment="Center" />
                                    <TextBlock Text="varoitusraja ylittynyt" Foreground="#BDBDBD" FontSize="11"
                                               Margin="4,0,14,0" VerticalAlignment="Center" />
                                    <Ellipse Width="10" Height="10" Fill="#EF5350" VerticalAlignment="Center" />
                                    <TextBlock Text="kriittinen raja ylittynyt" Foreground="#BDBDBD" FontSize="11"
                                               Margin="4,0,14,0" VerticalAlignment="Center" />
                                    <TextBlock Text="— samat värit korteissa, levyriveissä ja overlayn reunuksessa"
                                               Foreground="#BDBDBD" FontSize="11" VerticalAlignment="Center" />
                                </WrapPanel>
                            </StackPanel>
                        </Border>

                        <WrapPanel>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource CardTitle}" Text="CPU" />
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition /><RowDefinition /><RowDefinition /><RowDefinition />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Style="{StaticResource CardLabel}" Text="Käyttö" />
                                    <TextBlock Grid.Row="0" Grid.Column="1" Style="{StaticResource CardValue}" Text="{Binding CpuLoad}" />
                                    <TextBlock Grid.Row="1" Style="{StaticResource CardLabel}" Text="Lämpötila" />
                                    <TextBlock Grid.Row="1" Grid.Column="1" Style="{StaticResource CardValue}" Text="{Binding CpuTemp}"
                                               Foreground="{Binding CpuTempState, Converter={StaticResource StateBrush}}" />
                                    <TextBlock Grid.Row="2" Style="{StaticResource CardLabel}" Text="Kello (max)" />
                                    <TextBlock Grid.Row="2" Grid.Column="1" Style="{StaticResource CardValue}" Text="{Binding CpuClock}" />
                                    <TextBlock Grid.Row="3" Style="{StaticResource CardLabel}" Text="Teho" />
                                    <TextBlock Grid.Row="3" Grid.Column="1" Style="{StaticResource CardValue}" Text="{Binding CpuPower}" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource CardTitle}" Text="GPU" />
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition /><RowDefinition /><RowDefinition /><RowDefinition /><RowDefinition />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Style="{StaticResource CardLabel}" Text="Käyttö" />
                                    <TextBlock Grid.Row="0" Grid.Column="1" Style="{StaticResource CardValue}" Text="{Binding GpuLoad}" />
                                    <TextBlock Grid.Row="1" Style="{StaticResource CardLabel}" Text="Lämpötila" />
                                    <TextBlock Grid.Row="1" Grid.Column="1" Style="{StaticResource CardValue}" Text="{Binding GpuTemp}"
                                               Foreground="{Binding GpuTempState, Converter={StaticResource StateBrush}}" />
                                    <TextBlock Grid.Row="2" Style="{StaticResource CardLabel}" Text="Hotspot" />
                                    <TextBlock Grid.Row="2" Grid.Column="1" Style="{StaticResource CardValue}" Text="{Binding GpuHotspot}"
                                               Foreground="{Binding GpuHotspotState, Converter={StaticResource StateBrush}}" />
                                    <TextBlock Grid.Row="3" Style="{StaticResource CardLabel}" Text="VRAM" />
                                    <TextBlock Grid.Row="3" Grid.Column="1" Style="{StaticResource CardValue}" Text="{Binding GpuVram}" />
                                    <TextBlock Grid.Row="4" Style="{StaticResource CardLabel}" Text="Teho" />
                                    <TextBlock Grid.Row="4" Grid.Column="1" Style="{StaticResource CardValue}" Text="{Binding GpuPower}" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource CardTitle}" Text="RAM" />
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition /><RowDefinition />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Style="{StaticResource CardLabel}" Text="Käyttö" />
                                    <TextBlock Grid.Row="0" Grid.Column="1" Style="{StaticResource CardValue}" Text="{Binding RamLoad}"
                                               Foreground="{Binding RamLoadState, Converter={StaticResource StateBrush}}" />
                                    <TextBlock Grid.Row="1" Style="{StaticResource CardLabel}" Text="Käytössä" />
                                    <TextBlock Grid.Row="1" Grid.Column="1" Style="{StaticResource CardValue}" Text="{Binding RamUsed}" />
                                </Grid>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource CardTitle}" Text="Levyt" />
                                <ItemsControl ItemsSource="{Binding Disks}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Text}"
                                                       Foreground="{Binding State, Converter={StaticResource StateBrush}}"
                                                       FontFamily="Consolas" Margin="0,2" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <Border Style="{StaticResource Card}">
                            <StackPanel>
                                <TextBlock Style="{StaticResource CardTitle}" Text="Tuulettimet" />
                                <TextBlock Text="Kaksoisklikkaa nimeä antaaksesi oman nimen"
                                           Foreground="#BDBDBD" FontSize="11" Margin="0,0,0,6" />
                                <ItemsControl ItemsSource="{Binding Fans}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <TextBlock Text="{Binding DisplayName}" Foreground="#E0E0E0"
                                                           FontFamily="Consolas"
                                                           ToolTip="Kaksoisklikkaa nimetäksesi"
                                                           MouseLeftButtonDown="FanName_MouseLeftButtonDown">
                                                    <TextBlock.Style>
                                                        <Style TargetType="TextBlock">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBlock.Style>
                                                </TextBlock>
                                                <TextBox Text="{Binding EditText, UpdateSourceTrigger=PropertyChanged}"
                                                         Background="#1E1E1E" Foreground="White" FontFamily="Consolas"
                                                         BorderBrush="#4FC3F7"
                                                         KeyDown="FanName_KeyDown" LostFocus="FanName_LostFocus"
                                                         IsVisibleChanged="FanName_IsVisibleChanged">
                                                    <TextBox.Style>
                                                        <Style TargetType="TextBox">
                                                            <Setter Property="Visibility" Value="Collapsed" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsEditing}" Value="True">
                                                                    <Setter Property="Visibility" Value="Visible" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </TextBox.Style>
                                                </TextBox>
                                                <TextBlock Grid.Column="1" Text="{Binding Rpm}" Foreground="#A5D6A7"
                                                           FontFamily="Consolas" Margin="16,0,0,0" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        </WrapPanel>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Kaikki sensorit">
                <!-- Sensoripuu -->
                <TreeView ItemsSource="{Binding Hardware}"
                          Background="#1E1E1E"
                          BorderThickness="0"
                          Foreground="White"
                          FontFamily="Consolas"
                          FontSize="13"
                          Padding="12">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsExpanded" Value="True" />
                        </Style>
                    </TreeView.ItemContainerStyle>
                </TreeView>
            </TabItem>

            <TabItem Header="Asetukset">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="16" MaxWidth="780" HorizontalAlignment="Left">
                        <StackPanel.Resources>
                            <Style TargetType="GroupBox">
                                <Setter Property="Foreground" Value="#4FC3F7" />
                                <Setter Property="Margin" Value="0,0,0,16" />
                                <Setter Property="Padding" Value="12" />
                            </Style>
                            <Style x:Key="NumField" TargetType="TextBox">
                                <Setter Property="Width" Value="70" />
                                <Setter Property="Margin" Value="0,0,8,0" />
                                <Setter Property="Padding" Value="4,2" />
                                <Setter Property="Background" Value="#2D2D30" />
                                <Setter Property="Foreground" Value="#E0E0E0" />
                                <Setter Property="BorderBrush" Value="#9E9E9E" />
                                <Setter Property="VerticalAlignment" Value="Center" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasError}" Value="True">
                                        <Setter Property="BorderBrush" Value="#F44336" />
                                        <Setter Property="BorderThickness" Value="2" />
                                        <Setter Property="ToolTip" Value="{Binding Error}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                            <Style x:Key="ErrorText" TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#FF8A80" />
                                <Setter Property="TextWrapping" Value="Wrap" />
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </Trigger>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                            <Style x:Key="NoteText" TargetType="TextBlock">
                                <Setter Property="Foreground" Value="#BDBDBD" />
                                <Setter Property="FontStyle" Value="Italic" />
                                <Style.Triggers>
                                    <Trigger Property="Text" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </Trigger>
                                    <Trigger Property="Text" Value="">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                            <DataTemplate x:Key="ThresholdRowTemplate">
                                <Grid Margin="0,3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="190" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="40" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Label}" Foreground="#E0E0E0"
                                               VerticalAlignment="Center" />
                                    <TextBox Grid.Column="1" Style="{StaticResource NumField}"
                                             DataContext="{Binding Warn}" Text="{Binding Text}" />
                                    <TextBox Grid.Column="2" Style="{StaticResource NumField}"
                                             DataContext="{Binding Crit}" Text="{Binding Text}" />
                                    <TextBlock Grid.Column="3" Text="{Binding Unit}" Foreground="#BDBDBD"
                                               VerticalAlignment="Center" />
                                    <StackPanel Grid.Column="4" VerticalAlignment="Center" Margin="8,0,0,0">
                                        <TextBlock Style="{StaticResource ErrorText}" Text="{Binding Warn.Error}" />
                                        <TextBlock Style="{StaticResource ErrorText}" Text="{Binding Crit.Error}" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                            <DataTemplate x:Key="FieldRowTemplate">
                                <Grid Margin="0,3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="270" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="40" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{Binding Label}" Foreground="#E0E0E0"
                                               VerticalAlignment="Center" />
                                    <TextBox Grid.Column="1" Style="{StaticResource NumField}"
                                             DataContext="{Binding Field}" Text="{Binding Text}" />
                                    <TextBlock Grid.Column="2" Text="{Binding Unit}" Foreground="#BDBDBD"
                                               VerticalAlignment="Center" />
                                    <StackPanel Grid.Column="3" VerticalAlignment="Center" Margin="8,0,0,0">
                                        <TextBlock Style="{StaticResource ErrorText}" Text="{Binding Field.Error}" />
                                        <TextBlock Style="{StaticResource NoteText}" Text="{Binding Note}" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </StackPanel.Resources>

                        <GroupBox Header="Yleiset">
                            <StackPanel>
                                <CheckBox Content="Pienennä trayhin" IsChecked="{Binding MinimizeToTray}"
                                          Foreground="White" Margin="0,2" />
                                <CheckBox Content="Käynnistä Windowsin mukana" IsChecked="{Binding AutoStart}"
                                          Foreground="White" Margin="0,2" />
                                <CheckBox Content="Hälytysilmoitukset" IsChecked="{Binding AlertNotificationsEnabled}"
                                          Foreground="White" Margin="0,2"
                                          ToolTip="Näytä ilmoitus ilmaisinalueella, kun varoitus- tai kriittinen raja ylittyy" />
                                <StackPanel Orientation="Horizontal" Margin="0,8,0,2">
                                    <TextBlock Text="Kieli / Language" Foreground="#E0E0E0" Width="120"
                                               VerticalAlignment="Center" />
                                    <ComboBox SelectedIndex="{Binding SettingsPage.LanguageIndex}" Width="130"
                                              VerticalAlignment="Center">
                                        <ComboBoxItem Content="Automaattinen" />
                                        <ComboBoxItem Content="Suomi" />
                                        <ComboBoxItem Content="English" />
                                    </ComboBox>
                                    <TextBlock Style="{StaticResource NoteText}"
                                               Text="vaikuttaa uudelleenkäynnistyksen jälkeen"
                                               VerticalAlignment="Center" Margin="8,0,0,0" />
                                </StackPanel>
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Raja-arvot">
                            <StackPanel>
                                <Grid Margin="0,0,0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="190" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="80" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="1" Text="Varoitus" Foreground="#FFB74D"
                                               FontWeight="SemiBold" />
                                    <TextBlock Grid.Column="2" Text="Kriittinen" Foreground="#FF8A80"
                                               FontWeight="SemiBold" />
                                </Grid>
                                <ItemsControl ItemsSource="{Binding SettingsPage.ThresholdRows}"
                                              ItemTemplate="{StaticResource ThresholdRowTemplate}" />
                                <Button Content="Palauta oletusrajat" Click="ResetThresholds_Click"
                                        HorizontalAlignment="Left" Margin="0,8,0,0" Padding="10,3"
                                        ToolTip="Palauttaa raja-arvot ja kestot ohjelman oletuksiin" />
                            </StackPanel>
                        </GroupBox>

                        <GroupBox Header="Kestot">
                            <ItemsControl ItemsSource="{Binding SettingsPage.DurationRows}"
                                          ItemTemplate="{StaticResource FieldRowTemplate}" />
                        </GroupBox>

                        <GroupBox Header="Lokitus">
                            <ItemsControl ItemsSource="{Binding SettingsPage.LoggingRows}"
                                          ItemTemplate="{StaticResource FieldRowTemplate}" />
                        </GroupBox>

                        <GroupBox Header="Overlay">
                            <StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                    <TextBlock Text="Kulma" Foreground="#E0E0E0" Width="120"
                                               VerticalAlignment="Center" />
                                    <ComboBox SelectedIndex="{Binding OverlayCornerIndex}" Width="110"
                                              VerticalAlignment="Center">
                                        <ComboBoxItem Content="Vasen ylä" />
                                        <ComboBoxItem Content="Oikea ylä" />
                                        <ComboBoxItem Content="Vasen ala" />
                                        <ComboBoxItem Content="Oikea ala" />
                                    </ComboBox>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                    <TextBlock Text="Läpinäkyvyys" Foreground="#E0E0E0" Width="120"
                                               VerticalAlignment="Center" />
                                    <Slider Value="{Binding OverlayOpacity}" Minimum="0.3" Maximum="1.0"
                                            Width="140" VerticalAlignment="Center" />
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="0,2">
                                    <TextBlock Text="Fonttikoko" Foreground="#E0E0E0" Width="120"
                                               VerticalAlignment="Center" />
                                    <TextBox Style="{StaticResource NumField}"
                                             DataContext="{Binding SettingsPage.OverlayFontSize}"
                                             Text="{Binding Text}" />
                                    <TextBlock Style="{StaticResource ErrorText}"
                                               Text="{Binding SettingsPage.OverlayFontSize.Error}"
                                               VerticalAlignment="Center" Margin="8,0,0,0" />
                                </StackPanel>
                                <TextBlock Text="Näytettävät mittarit" Foreground="#E0E0E0"
                                           Margin="0,8,0,2" />
                                <WrapPanel>
                                    <CheckBox Content="CPU" IsChecked="{Binding OverlayShowCpu}"
                                              Foreground="White" Margin="0,0,12,0" />
                                    <CheckBox Content="GPU" IsChecked="{Binding OverlayShowGpu}"
                                              Foreground="White" Margin="0,0,12,0" />
                                    <CheckBox Content="RAM" IsChecked="{Binding OverlayShowRam}"
                                              Foreground="White" Margin="0,0,12,0" />
                                    <CheckBox Content="Levyt" IsChecked="{Binding OverlayShowDisks}"
                                              Foreground="White" Margin="0,0,12,0" />
                                    <CheckBox Content="Tuulettimet" IsChecked="{Binding OverlayShowFans}"
                                              Foreground="White" Margin="0,0,12,0" />
                                </WrapPanel>
                            </StackPanel>
                        </GroupBox>
                    </StackPanel>
                </ScrollViewer>
            </TabItem>

            <TabItem Header="Historia">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="16,12,16,4">
                        <TextBlock Text="Aikaväli" Foreground="#BDBDBD" VerticalAlignment="Center"
                                   Margin="0,0,8,0" />
                        <ComboBox SelectedIndex="{Binding History.RangeIndex}" Width="90"
                                  VerticalAlignment="Center">
                            <ComboBoxItem Content="1 h" />
                            <ComboBoxItem Content="24 h" />
                            <ComboBoxItem Content="7 pv" />
                            <ComboBoxItem Content="30 pv" />
                        </ComboBox>
                        <TextBlock Text="Graafit päivittyvät automaattisesti minuutin välein"
                                   Foreground="#BDBDBD" FontStyle="Italic"
                                   VerticalAlignment="Center" Margin="16,0,0,0" />
                    </StackPanel>
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="16,4,16,16">
                            <TextBlock Text="Lämpötilat (°C)" FontSize="15" FontWeight="Bold"
                                       Foreground="#4FC3F7" Margin="0,8,0,4" />
                            <lvc:CartesianChart Background="#252526"
                                                Series="{Binding History.TempSeries}"
                                                XAxes="{Binding History.TempXAxes}"
                                                YAxes="{Binding History.TempYAxes}"
                                                LegendPosition="Right"
                                                LegendTextPaint="{Binding History.LegendPaint}"
                                                TooltipTextPaint="{Binding History.TooltipTextPaint}"
                                                TooltipBackgroundPaint="{Binding History.TooltipBackgroundPaint}"
                                                Height="280" />
                            <TextBlock Text="Kuormat (%)" FontSize="15" FontWeight="Bold"
                                       Foreground="#4FC3F7" Margin="0,16,0,4" />
                            <lvc:CartesianChart Background="#252526"
                                                Series="{Binding History.LoadSeries}"
                                                XAxes="{Binding History.LoadXAxes}"
                                                YAxes="{Binding History.LoadYAxes}"
                                                LegendPosition="Right"
                                                LegendTextPaint="{Binding History.LegendPaint}"
                                                TooltipTextPaint="{Binding History.TooltipTextPaint}"
                                                TooltipBackgroundPaint="{Binding History.TooltipBackgroundPaint}"
                                                Height="240" />
                            <TextBlock Text="Tuulettimet (RPM)" FontSize="15" FontWeight="Bold"
                                       Foreground="#4FC3F7" Margin="0,16,0,4" />
                            <lvc:CartesianChart Background="#252526"
                                                Series="{Binding History.FanSeries}"
                                                XAxes="{Binding History.FanXAxes}"
                                                YAxes="{Binding History.FanYAxes}"
                                                LegendPosition="Right"
                                                LegendTextPaint="{Binding History.LegendPaint}"
                                                TooltipTextPaint="{Binding History.TooltipTextPaint}"
                                                TooltipBackgroundPaint="{Binding History.TooltipBackgroundPaint}"
                                                Height="240" />
                        </StackPanel>
                    </ScrollViewer>
                </DockPanel>
            </TabItem>
        </TabControl>
    </DockPanel>
</Window>
